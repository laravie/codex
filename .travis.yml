language: php
sudo: false
dist: trusty

php:
  - 7.1
  - 7.2
  - 7.3
  - nightly

env:
  global:
    - coverage=no
  matrix:
    - httplug=2.0
    - setup=lowest httplug=2.0
    - setup=stable httplug=2.0

before_script:
  - travis_retry composer self-update
  - composer config discard-changes true
  - travis_retry composer require "php-http/httplug=^${httplug}" --prefer-dist --no-interaction --dev --no-suggest
  - if [[ $setup = 'stable' ]]; then travis_retry composer update --prefer-source --no-interaction --prefer-stable --no-suggest; fi
  - if [[ $setup = 'lowest' ]]; then travis_retry composer update --prefer-source --no-interaction --prefer-lowest --prefer-stable --no-suggest; fi
  - if [[ $setup = 'coveralls' ]]; then travis_retry composer require "php-coveralls/php-coveralls=^2.1" --prefer-dist --no-interaction --dev --no-suggest; fi

script:
  - if [[ $coverage = 'yes' ]]; then vendor/bin/phpunit -c phpunit.xml --coverage-clover build/logs/clover.xml; fi
  - if [[ $coverage = 'no' ]]; then vendor/bin/phpunit -c phpunit.xml; fi

after_script:
  - if [[ $setup = 'coveralls' ]]; then php vendor/bin/php-coveralls -v; fi

matrix:
  include:
    - php: 7.1
      env: setup=coveralls coverage=yes httplug=2.0
  allow_failures:
    - php: nightly
    - env: setup=coveralls coverage=yes httplug=2.0
  fast_finish: true
